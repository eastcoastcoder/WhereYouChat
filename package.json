{
  "scripts": {
    "build": "parcel build index.html&&rm -rf docs&&mv dist docs"
  },
  "dependencies": {
    "@mapbox/leaflet-omnivore": "^0.3.4",
    "leaflet": "^1.3.4",
    "leaflet-draw": "^1.0.3"
  },
  "devDependencies": {
    "cssnano": "^4.1.4"
  }
}
